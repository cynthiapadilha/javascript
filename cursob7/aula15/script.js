/*function maiorDeIdade (idade){
    if (idade >= 18){
        return true;
    }else{
        return false;
    }
}
*/

/*console.log (maiorDeIdade(10)) // posso mostrar o resultado dessa forma*/

//let verificacao = maiorDeIdade(40); //ou dessa forma

/*if (verificacao){
    console.log ('É maior de idade');
}else {
    console.log ('É menor de idade');
}
*/

function gravidade(num){
    let resultado = num
    return num
}
let num = 9.8

let resultado = gavidade (num)



console.log (resultado)


/*function calcPct( x , y ){
    let pct = (y / x)*100
    return pct
}
let x =40 
let y=10

let pct = calcPct (x , y)
console.log(`${pct}% de ${x} é ${y}`)
